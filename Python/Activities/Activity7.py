elements=list(input("Enter all the elements in the list: "))
print(f"The elements in the list are {elements}")
sum=0
for element in elements:
    sum+=int(element)
print(f"The sume of the elements in the list is {sum}")